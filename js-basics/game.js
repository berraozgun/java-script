const secretNumber= Math.floor(Math.random() * 100);

let userGuess=prompt("Please guess the number");

while (number != secretNumber) {
    let number = prompt ("Guess teh number");
    if (number )
}